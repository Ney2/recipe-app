<h1>New Ingredient</h1>

<%= form_with(model: @recipe_food) do |form| %>
  <% if @recipe_food.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(@recipe_food.errors.count, "error") %> prohibited this ingredient from being saved:</h2>

      <ul>
        <% @recipe_food.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>


  <div class="mt-5">
    <%= form.label :recipe_id, style: "display: block" %>
      <%=form.collection_select :recipe_id, Recipe.order(:created_at), :id, :name %>

  </div>

  <div class="mt-5">
    <%= form.label :food_id, style: "display: block" %>
      <%=form.collection_select :food_id, Food.order(:created_at), :id, :name  %>
  </div>
  <div class="mt-5">
    <%= form.label :quantity, style: "display: block" %>
    <%= form.number_field :quantity %>
  </div>

  <div class="mt-5">
    <%= form.submit %>
  </div>
<% end %>

